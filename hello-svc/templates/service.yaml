apiVersion: v1
kind: Service
metadata:
  name: {{ template "hello-svc.fullname" . }}
  labels:
    app: {{ template "hello-svc.name" . }}
    chart: {{ template "hello-svc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hello-svc.name" . }}
    release: {{ .Release.Name }}
